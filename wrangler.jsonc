{
  // Static assets only — there is no Worker script, so no "main" entry point.
  // The app is a plain SPA build; Cloudflare serves dist/ directly.
  "name": "forge",
  "compatibility_date": "2026-08-27",
  "assets": {
    "directory": "./dist"
  }
}
